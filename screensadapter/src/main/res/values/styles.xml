<resources>

    <style name="StarterTheme" parent="@style/Base.Theme.AppCompat">
        <item name="android:windowBackground">@drawable/sa_bg_splash</item>
    </style>

    <style name="AppTheme.Dialog" parent="Base.Theme.AppCompat.Light.Dialog">
        <!-- removing the dialog's action bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
